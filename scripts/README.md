# Scripts

The first real importer will live here after the first paper draft is ready.

Planned command:

```bash
npm run import:draft -- /path/to/paper-project/blogs.md
```

The importer should ask for missing metadata, copy assets, and write bilingual MDX posts into `src/content/post/en` and `src/content/post/zh`.

